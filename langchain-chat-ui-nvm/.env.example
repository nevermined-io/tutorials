# ---------------------------------------------------------------------------
# Nevermined x402 — server-side only
# ---------------------------------------------------------------------------

# Your personal Nevermined API key (do NOT prefix with NEXT_PUBLIC_ —
# this must stay server-side). Generated at https://nevermined.app under
# Settings → API Keys.
NVM_API_KEY=

# The organization id the widget session will be scoped to. You must be
# an active member of this org. Find it under Settings → Organizations on
# nevermined.app, or via `payments.organizations.getMyMemberships()`.
NVM_ORG_ID=

# Nevermined environment: sandbox | live | staging_sandbox | staging_live
# Defaults to "sandbox" when unset. Determines both the backend URL the
# server hits (`/api/v1/widgets/session/self`) and the standalone embed
# app the popup opens (`https://embed.<tier-host>/cards/setup`).
NVM_ENVIRONMENT=sandbox

# Optional override for the embed-app origin. By default the chat UI
# derives it from NVM_ENVIRONMENT (prepending `embed.` to the SDK's
# frontend host). Set this if you run a custom embed deployment or for
# `NVM_ENVIRONMENT=custom`. No trailing slash.
# Example: NVM_EMBED_URL=https://embed.nevermined.dev
NVM_EMBED_URL=

# Plan id the agent's `market_research` tool charges against. Same value
# the agent's own .env carries as NVM_PLAN_ID. The chat UI uses it to
# mint an x402 access token (the agent gates inside the tool, so there
# is no longer a 402 envelope to discover this from).
NVM_PLAN_ID=

# Optional: agent id this plan is associated with. When set, surfaces on
# the x402 token request's `extra.agentId` and on the buyer-side init
# response so the chat UI can render what the user is paying for.
NVM_AGENT_ID=

# ---------------------------------------------------------------------------
# LangGraph backend (Sprint 3 agent — proxied through /api/*)
# ---------------------------------------------------------------------------

# The LangSmith Deployment / `langgraph dev` URL of the gated agent. The
# Next.js catch-all proxy at /api/* forwards browser requests here while
# injecting payment-signature from a httpOnly cookie. Do NOT prefix this
# with NEXT_PUBLIC_ — server-side only.
LANGGRAPH_API_URL=http://127.0.0.1:2024

# ---------------------------------------------------------------------------
# Chat-UI (browser-visible)
# ---------------------------------------------------------------------------

# Tell the LangGraph SDK to talk to our local proxy at /api, NOT directly
# to the agent. The proxy is what injects payment-signature.
NEXT_PUBLIC_API_URL=http://localhost:3000/api

# Graph / assistant id served by the agent. Defaults to "research" in
# ~/code/tutorials/langchain-langsmith-deployment-py/langgraph.json.
NEXT_PUBLIC_ASSISTANT_ID=research

# Leave blank for local dev — the upstream chat UI uses this for
# LangSmith Deployment Agent Builder auth, which is out of scope here.
NEXT_PUBLIC_AUTH_SCHEME=
