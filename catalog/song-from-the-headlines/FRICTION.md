# Developer friction log

| Step | Expected | Observed and reproduction | Impact | Fix and acceptance |
| --- | --- | --- | --- | --- |
| Router receipt | A settled charge prints in dollars. | On the first 2026-09-15 Live run, `amount:35000` (six-decimal USDC atomic units) printed as `$35000` for a $0.035 Brave charge. Run the former receipt expression against the Router payments response. | Builders cannot tell whether the run stayed within its budget. | `run-demo.sh` now divides `amount` by 1,000,000. A local fixture and the second bounded Live run printed `$0.035` for Brave; resolved. |
| Suno completion | A paid generation job produces a downloadable song at roughly its advertised generation price. | The same run needed five Router-paid status checks at $0.005 each after a $0.105 generation charge. A `taskId` arrived before the audio URL. | Generation alone understates total spend and does not prove the deliverable. | The runner allows at most 12 status checks under a 50¢ delegation and requires an HTTPS audio URL and nonempty downloaded file. Accept when a repeat produces the audio and all polling charges appear in the receipt. |
| Publishing an example | A repeat preserves the existing public representative media until new files are reviewed. | The former runner wrote directly to checked-in `song.mp3` and `album-cover.jpg`. | A failed or unreviewed repeat could replace working public samples. | New media goes to ignored `out/`; both September 15 repeats left tracked media untouched. Reviewed artifacts can be published deliberately; runner fix resolved. |

The first September 15 run returned audio and cover and saved both files privately; nine payments Settled for $0.1705. A second bounded Live run after the receipt and timing fixes also delivered both artifacts, printed correct dollar amounts and an 8-second first-result time, and settled seven payments totaling $0.1605. Both runs used two rails and chains. The artifact files remain unreviewed and unpublished.
