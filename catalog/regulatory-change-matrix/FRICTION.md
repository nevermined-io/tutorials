# DevEx friction log

This log includes public discovery and one bounded live Router attempt on 2026-09-15. No merchant payment, paid GovLaws response, real matrix, fix, or repeat run has occurred. **The outcome remains blocked.**

| Step | Expected | Observed / reproduction | Impact | Proposed fix and verification |
| --- | --- | --- | --- | --- |
| Discover `govlaws-mpp` schema | `get_service` or the Catalog detail provides request and response schemas for each path | `GET https://api.live.nevermined.dev/api/v1/catalog/services/govlaws-mpp` exposes method, path, description, and price, but no input/output schema. MCP `get_service` relays the same DTO. The [vendor OpenAPI](https://govlaws.ai/openapi.json) does provide typed `ChangesQueryInput` and `ResolveCitationInput`. | A builder cannot copy a complete payable request from the Catalog alone; upstream docs may not match the Router wrapper. | Add optional schema or official OpenAPI link to service metadata and render it in detail/MCP. Acceptance: a fresh discovery yields a link or typed query/body fields for both paths. |
| Plan a budget | Catalog endpoint prices agree with current merchant price | Catalog lists `/api/mpp/changes` at $0.03 and `/api/mpp/resolve` at $0.05; [GovLaws MPP docs](https://govlaws.ai/mpp) list $0.06 and $0.05. Read both pages on 2026-09-15. | An outcome ledger built from Catalog labels understates one call by $0.03. | Refresh posted price labels and show quote time/source. Acceptance: Catalog labels match the merchant's current posted figures, with live 402 quote identified as final authority. |
| Inspect quote before payment | The builder can receive a quote, compare it with local budget, then authorize | Router slug mode B handles the merchant 402 and signs inside one server-side relay. The public Catalog DTO only carries a label; the caller cannot see the exact in-band quote before the purchase. | A strict per-call client-side budget gate is impossible using this flow alone. The delegation cap remains the payment guard. | Expose a non-spending quote/preflight API bound to slug/path/parameters, or a maximum-charge field enforced before signing. Acceptance: a request over a supplied max is rejected without a payment record. |
| Explain the GovLaws changes response | A priced changes query challenges with 402 and returns a payable body after settlement | The bounded 2026-09-15 request returned HTTP 200 with an empty raw body and no payment ID; the broker deliberately withheld the free/non-402 cataloged response. No merchant payment was recorded. | The tutorial cannot establish a payable changes response or complete a matrix. | Ask the merchant why this priced endpoint answered without a 402 and publish a working payable query. Acceptance: a documented changes call returns a paid body and matching Settled ledger record, then resolve can be validated under a fresh cap. |
| Validate payable wrapper | A public fixture shows exact `/api/mpp/changes` and `/api/mpp/resolve` Router inputs and paid response fields | The sample uses GovLaws OpenAPI queries and Router's host-locked slug path. The bounded first changes call did not reach a verified GovLaws result, and no paid resolve call has been attempted. | The live outcome remains blocked and cannot be advertised as reproduced. | After broker correction, run a capped changes call and one resolve call, record exact returned fields and spend, and repeat the journey after the fix. |

No linked issues or fixes are claimed here; owners and issue links should be added once the Catalog team triages these findings.
