# =============================================================================
# Nevermined Configuration (required for both agent and client)
# =============================================================================

# Your Nevermined API key (get it from https://nevermined.app)
NVM_API_KEY=nvm:your-api-key

# Environment: 'sandbox' (Base Sepolia) or 'live' (Arbitrum One)
NVM_ENVIRONMENT=sandbox

# The Plan ID that protects the /ask endpoint
NVM_PLAN_ID=your-plan-id

# =============================================================================
# Agent Configuration (server)
# =============================================================================

# OpenAI API Key for the AI endpoint
OPENAI_API_KEY=sk-your-openai-api-key

# Server port (optional, defaults to 3000)
PORT=3000

# =============================================================================
# Client Configuration
# =============================================================================

# URL of the agent server (optional, defaults to http://localhost:3000)
SERVER_URL=http://localhost:3000
